#!/bin/sh
# curl test double that simulates exit 7, "Failed to connect to host".
# Used by the unreachable-Infisical tests. Drains stdin so the loader's
# `printf ... | curl` never blocks, then fails the way a real curl does.
cat >/dev/null 2>&1 || true
exit 7
