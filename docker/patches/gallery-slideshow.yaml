# Class C — inherited BlueSpice patch. Generated from the .diff; see
# docs/dev/patches.md for the inventory and the verification contract.
id: gallery-slideshow
title: resolve gallery slideshow titles from the link, not the img
class: C
mode: diff
target: resources/src/mediawiki.page.gallery.slideshow.js
patch: app/_bluespice/patches/resources/src/mediawiki.page.gallery.slideshow.js.diff
applied_by: app/_bluespice/pre-autoload-dump.d/99-apply_patches.sh
hunks: 1
stale: false
group: ''
upstream_version: 'BlueSpice 5.1.9 (inherited patch set)'
regex: n/a
why: >-
  Inherited from upstream BlueSpice; not authored here. New in the 5.1.9
  patch set — the first addition to the inherited set since this manifest
  was written. mw.Title.newFromImg() only resolves a plain image gallery,
  so it derives the title from the enclosing link and keeps the old call
  as the fallback.
